#include <stdio.h>
#include <stdlib.h>
#include "Sorting.h"
#include "search.h"
#include "search_and_sort.h"

int BubbleSort(int n);
int Searching(int n);
int searchandsort(int n, int c);
int main()
{
    int n = 0;
    int c =0 ;

   // BubbleSort(n);
   // Searching(n);
 searchandsort(n,c);


    return 0;
}
