#include "Selection_sorting.h"

int main()
{
    uint32_t u32Arr[10] = {15,10,5,1,3,2,15,18,19,14};
    uint8_t u8Count = 0;

    Bubble_sort_array(u32Arr);

    for (u8Count = 0 ; u8Count < 10 ; u8Count++)
    {
        printf("%d \n",u32Arr[u8Count]);
    }

    return 0;
}
